"""
숫자 카드 게임

여러 개의 숫자 카드 중에서 가장 높은 숫자가 쓰인 카드 한 장을 뽑는 게임이다.
단, 카드를 뽑을 때는 다음의 규칙을 따라야 한다.

1. 숫자가 쓰인 카드들이 N x M 형태로 놓여 있다. 이때 N은 행의 개수를 의미하며,
   M은 열의 개수를 의미한다.
2. 먼저 뽑고자 하는 카드가 포함되어 있는 행을 선택한다.
3. 그다음 선택된 행에 포함된 카드들 중 가장 숫자가 낮은 카드를 뽑아야 한다.
4. 따라서 처음에 카드를 골라낼 행을 선택할 때, 이후에 해당 행에서 가장 숫자가 낮은
   카드를 뽑을 것을 고려하여 최종적으로 가장 높은 숫자의 카드를 뽑을 수 있도록
   전략을 세워야 한다.

카드들이 N x M 형태로 놓여 있을 때, 게임의 룰에 맞게 카드를 뽑는 프로그램을
작성하시오.

입력 조건
- 첫째 줄에 숫자 카드들이 놓인 행의 개수 N(1 <= N <= 100)과 열의 개수 M(1 <= M <= 100)이
  공백을 기준으로 하여 각각 자연수로 주어진다.
- 둘째 줄부터 N개의 줄에 걸쳐 각 카드에 적힌 숫자가 주어진다.
  각 숫자는 1 이상 10,000 이하의 자연수이다.

출력 조건
- 첫째 줄에 게임의 룰에 맞게 선택한 카드에 적힌 숫자를 출력한다.

예제 입력 1        예제 출력 1
3 3                2
3 1 2
4 1 4
2 2 2

예제 입력 2        예제 출력 2
2 4                3
7 3 1 8
3 3 3 4

── 힌트: 여러 줄 입력 받기 ──────────────────────────────────
지금까지는 input()을 두 번 썼지만, 이번엔 N개의 줄을 받아야 한다.
줄 수가 입력값(N)으로 정해지므로 반복문 안에서 input()을 부른다.

    for i in range(n):
        row = list(map(int, input().split()))
        # 이 row 로 무엇을 할지는 직접 생각해 보기

각 줄을 받을 때마다 바로 처리할 수도 있고, 전부 리스트에 모아둔 뒤
나중에 처리할 수도 있다. 둘 다 맞는 방법이다.
─────────────────────────────────────────────────────────────

테스트:
    printf '3 3\n3 1 2\n4 1 4\n2 2 2\n' | python3 practice/03/04_card_game.py
"""

n, m = map(int, input().split())
num = []

for i in range(n):
  row = list(map(int, input().split()))
  num.append(min(row))

print(max(num))  


##

for i in range(n):
  data = list(map(int, input().spilt()))
  min_value = min(data)
  result = max(result, min_value)

print(result)